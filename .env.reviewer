# Public, non-secret build flag. Use `npm run build:reviewer` only for the
# isolated hackathon reviewer deployment. Standard production builds leave the
# mock-auth presentation disabled.
VITE_REVIEWER_DEMO_MODE=true
