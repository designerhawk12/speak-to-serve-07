# Implementation Status

- Gemini Interactions response extraction was hardened and deployed as active `ai-gateway` Edge Function version 21 on 2026-08-28 without changing prompts, AI task schemas, Auth, RLS, grievance/routing logic, database schema, or UI workflows. The server now prefers the final usable `steps[].type = model_output`, concatenates all of that step's ordered `content[].type = text` blocks before JSON parsing, ignores thought content, retains `output_text`, `outputText`, and legacy `outputs[].text` compatibility, and permits `incomplete` envelopes to proceed only through the existing JSON/runtime-schema validation. Terminal failed/cancelled/requires-action envelopes still fall back. Gemini requests pin `Api-Revision: 2026-05-20`, retain `store: false`, and extraction failures log only bounded structural envelope metadata. Focused AI gateway tests pass (50 tests, 173 assertions), as do `npx tsc --noEmit`, `npm run build`, and the full suite (191 tests, 589 assertions).

- A1 intake UI simplification was completed on 2026-08-28 without changing Gemini, prompts, provider/gateway logic, taxonomy loading, routing backend, assignment, RLS, database schema, grievance lifecycle, or submission. AI interpretation continues to show the issue, requested outcome, detected location/identifiers, helpful information, suggested grievance type, and suggested government destination, but those values are now explicitly advisory. The destination step immediately exposes the existing manual category/organization selectors and explains that the suggestion is not selected automatically. The misleading “We couldn't automatically determine…” banner and the automatic suggested-route confirmation path were removed. Only a genuine absence of any interpretation uses the neutral “AI guidance is unavailable right now…” manual-continuation message. New suggestions no longer write category/organization IDs into the draft; the citizen confirms their manual choices before proceeding. Focused intake/draft/English-Hindi-Kannada interpretation tests pass (21 tests, 67 assertions).

- The final AI feature pass was completed and deployed as ACTIVE `ai-gateway` Edge Function version 20 on 2026-08-28 without changing Auth, schema, RLS, assignment, routing authority, grievance lifecycle, resolution submission, or adding an agent/RAG framework. `AI_RESOLUTION_COMPARE` now validates the full addressed/partial/likely-unresolved/insufficient assessment contract, compares the authoritative complaint/requested outcome with every resolution draft field and evidence reference, flags forwarding/processed/disposed boilerplate, distinguishes concrete completed action/work references, supports cross-language comparison, and records validated output/prompt/provider metadata in `ai_runs`. The gateway reads private cases only through the caller client; a GRO must be the current assignee and a Nodal Officer remains RLS-subtree scoped. Runtime checks reject binding legal/entitlement/closure conclusions. The officer UI displays the citizen request beside the claimed government action, unresolved/addressed points, evidence gap, improvement, warning, and confidence under “AI advisory — the officer remains responsible for the final response.” AI failure never disables manual submission, and editing the draft clears a stale comparison.
- The guidance assistant is now intentionally lightweight navigation/help only. It no longer accepts/selects a grievance or queries/summarizes private case data. Its validated result is one answer plus at most one actual allowlisted route/action label covering filing, My grievances, Track, sign-in/signup, notifications, appeal status, FAQ/About, and the new-grievance route. Invented URLs, inconsistent route/label pairs, and binding action claims are discarded. Unauthenticated case questions reveal no case fact and direct the visitor to privacy-safe tracking; deterministic filing/My grievances/Track/Appeal/FAQ guidance remains useful when Gemini is unavailable. A deployed public smoke check returned `/citizen/grievances/new` through that fallback, unauthenticated resolution comparison returned 403, and an authenticated assigned-GRO comparison reached Gemini (`fallback_used=false`) and returned `LIKELY_UNRESOLVED` with a generic-response warning. Focused AI tests pass (45 tests, 166 assertions), as do `npx tsc --noEmit`, `npm run build`, the complete suite (189 tests, 588 assertions), and focused lint.

- The final A1 route-selection state bug was repaired on 2026-08-28 without changing Gemini, prompts, taxonomy loading, Supabase schema/RLS, routing backend, assignment, or any grievance lifecycle. The remaining defect was in `applyIntakeInterpretation`: a stale `manualTaxonomy=true` flag prevented an otherwise validated AI organization/category route from replacing empty/manual draft fields. In addition, “Continue with this selection” committed a second set of display-derived suggestion objects instead of the authoritative draft route. A usable AI route now always writes its active organization ID and most-specific category row ID into the draft, clears manual mode, and makes `draftHasUsableAiRoute` the Continue/fallback condition. The existing schema stores a selected subcategory as `categoryId`/`grievances.category_id`; no duplicate `subcategoryId` state or schema column was introduced. Persisted drafts created by the earlier contradiction are repaired only when `fallback_used=false`, the IDs revalidate against current active taxonomy, and no complete citizen manual route exists. Change keeps manual selection authoritative, while explicitly rerunning analysis may apply a new valid AI route. Focused tests pass (18 tests, 49 assertions), as do `npx tsc --noEmit`, `npm run build`, and the full suite (175 tests, 529 assertions). Focused lint has no errors and retains only the intake route's two pre-existing Hook dependency warnings. The in-app browser still blocks localhost with `ERR_BLOCKED_BY_CLIENT`, so local browser automation could not be used as an additional acceptance signal.

- The A1 route-acceptance/eligibility conflict correction was completed and deployed as active `ai-gateway` Edge Function version 19 on 2026-08-28 without changing Auth, RLS, schema, assignment, transfer, or grievance lifecycle logic. The browser now validates and uses the gateway's active `suggested_organization_id` instead of discarding it and relying only on the selected category's default organization. Route presentation has three explicit states: `ai_resolved` (valid IDs at/above 0.65), `ai_review` (valid IDs below 0.65, still visible and continuable), and `manual_fallback` (provider fallback or no usable active route). Missing recommended information is independent of route acceptance. Server reconciliation evaluates explicit RTI, sub-judice, government-employee-service, religious/non-service, and suggestion eligibility before accepting a normal taxonomy route; those advisory types now return null route IDs rather than an unrelated nearest match. Intake audit prompt version is `grievance-intake-v2.0.1`. Focused regressions pass (45 tests, 140 assertions), as do `npm run build`, `npx tsc --noEmit`, and the full suite (171 tests, 513 assertions). Focused lint has no errors and retains only the intake route's two pre-existing Hook dependency warnings.

- The flagship `AI_GRIEVANCE_INTAKE` workflow was completed and deployed as active `ai-gateway` Edge Function version 18 on 2026-08-28 without changing Auth, RLS, database schema, assignment, routing authority, grievance lifecycle, or introducing LangGraph/LangChain. The citizen still begins with “What happened?”; one validated Gemini/provider call receives the current draft plus every active database organization, category/subcategory, hierarchy, plain-language hint, and category-to-organization default. The present taxonomy is small enough for one bounded call (12 active organizations and 10 active categories/subcategories); the documented contract permits two ordinary sequential calls after a future verified large import without an architecture redesign.
- Intake now returns detected `original_language`, citizen-language issue/structured summary/requested outcome/guidance, detected location/identifiers, advisory eligibility type, validated organization/category/subcategory IDs, route explanation/confidence, and REQUIRED/RECOMMENDED/OPTIONAL completion lanes. The server strictly validates the structure, discards invented IDs/labels, re-resolves the route from live active rows, derives the organization through the stored category relationship, and converts every model-declared required item into a recommendation because only deterministic product rules may require form data. Original citizen text is never replaced and is submitted without trimming.
- English, Hindi, Gujarati, Marathi, Bengali, Telugu, Assamese, Odia, Tamil, Malayalam, Urdu, Sindhi, Bodo, Konkani, Nepali, Manipuri, Punjabi, Kannada, Dogri, Maithili, Kashmiri, Sanskrit, and Santhali are exposed through the shared language preference contract. The provider uses one multilingual prompt; there are no per-language prompt files. `und` remains available for genuinely unknown input.
- The interpretation review now presents “WE UNDERSTOOD YOUR PROBLEM AS,” “WHAT YOU WANT RESOLVED,” the suggested grievance type, and suggested government destination. A complete validated route offers “Continue with this selection”; Change opens the existing searchable database-backed selectors. Route confidence below the central 0.65 threshold shows a review warning without discarding a valid candidate. Provider failure or malformed output preserves the complaint, shows the safe fallback message, and automatically opens manual routing rather than silently accepting the deterministic fallback.
- Advisory intake types cover actionable grievances, possible RTI, possible sub-judice matters, government-employee service matters, religious/non-service matters, suggestions, and uncertainty. Uncertain or potentially out-of-scope text remains continuable. Prompt-injection text is explicitly treated as complaint data and grants no authority to invent a taxonomy route or perform a government action. Every completed intake path retains the existing `ai_runs` audit with prompt version `grievance-intake-v2.0.0`, provider/model, validated structured output, route confidence, and no chain-of-thought or secrets.
- Focused regression coverage includes English pension and streetlight/location cases, Hindi pension output, Kannada and Tamil streetlight output, RTI, suggestion, ambiguous input, malicious routing instructions, invalid/invented taxonomy IDs, deterministic required-field demotion, low-confidence presentation, provider failure/manual routing, Unicode/source-language preservation, all exposed languages, and the actual wizard labels/actions. `npx tsc --noEmit`, `npm run build`, and `npm run test` pass (166 tests, 496 assertions); focused lint has no errors and retains only the route's two pre-existing Hook dependency warnings. A post-deploy external payload smoke test was not run because execution approval for sending even a synthetic complaint to the remote provider was denied; deployment bundling and active version status were verified through the connected Supabase CLI.

- Gemini raw REST response parsing was repaired on 2026-08-28 without changing the AI gateway architecture or any workflow/data authorization contract. The Gemini adapter now requires a completed interaction and extracts only the final non-empty `steps[].type = model_output` / `content[].type = text` value; thought/non-model steps are ignored. Empty, failed, cancelled, incomplete, requires-action, malformed-JSON, schema-invalid, and HTTP-error responses throw a bounded server-side provider failure and use the existing deterministic/manual fallback. Gemini requests now include `store: false` and never use conversation persistence. Server-only fallback diagnostics record provider, model, HTTP status/code where safely supplied, and the precise failure stage (`HTTP`, timeout, response extraction, JSON parse, or Zod/schema validation), without keys, headers, raw grievance text, or direct PII. Focused raw-REST parser tests, TypeScript, build, and the full test suite pass.

- Gemini support was added to the existing single server-side `ai-gateway` on 2026-08-28 without changing the gateway task contracts, taxonomy loading, `ai_runs` table, Auth, RLS, routing, assignment, or grievance lifecycle. `AI_PROVIDER=gemini`, `GEMINI_API_KEY`, and configurable `AI_MODEL` select the Gemini structured-output adapter; the optional OpenAI adapter remains available when explicitly selected. Gemini requests execute only in the Supabase Edge Function, use the existing bounded timeout/retry controls, and their output still passes task-specific strict Zod validation before use. Missing Gemini configuration, provider errors, and malformed output retain deterministic/manual fallback. Audit records now include explicit provider/model metadata inside the validated audit payload as well as the existing `model_label`. Public intake remains public; anonymous officer summary/resolution comparison are rejected before provider use, and authorized case reads remain caller-scoped/RLS-bound. The function is deployed and a live anonymous intake request safely returned the deterministic fallback because no provider-backed response was configured. Verification passes: `npx tsc --noEmit`, `npm run build`, and `npm run test` (150 tests, 440 assertions). Focused tests cover provider selection, missing key fallback, valid/malformed structured output, unauthenticated task boundary, citizen ownership, GRO/Nodal role boundary, and client-source secret absence.

- The final bounded AI implementation was deployed to the existing `ai-gateway` Edge Function on 2026-08-28 without a schema, RLS, Auth, routing, assignment, lifecycle, priority, clustering, LangGraph, LangChain, vector, embedding, or autonomous-agent change. One server-only gateway now exposes runtime-validated advisory tasks: `grievance_intake` for multilingual intake understanding/requested-outcome extraction/missing-information checks and active-taxonomy suggestions; `officer_summary` for GRO/Nodal RLS-authorized case summaries; `resolution_compare` for non-blocking request/response gap and generic-wording checks; and `translate` for presentation-only translation. The intake route uses the gateway, retains the original citizen text, and falls back to the database-driven local matcher/manual routing if unavailable. The office file has a manual advisory summary control, and the resolution composer has a manual advisory comparison control that cannot submit or approve a response. Every task has timeout/retry/provider validation, PII-aware provider input, safe fallback, prompt versioning, and an `ai_runs` audit record with PII-redacted structured output but no raw prompt/chain-of-thought. A live deployed intake call and audit check passed. The taxonomy audit found 12 active organizations (9 nested), 10 active categories (5 subcategories), and 10 category defaults across 5 organizations; `TAXONOMY_DATA_INCOMPLETE = YES` because the rows include prototype/demo data rather than a verified nationwide dataset. `docs/TAXONOMY_STATUS.md` records the count and the explicit all-active-row paging contract. Verification passes: `npm run build`, `npx tsc --noEmit`, and `npm run test` (144 tests, 419 assertions). Focused AI lint has no errors; the existing intake route retains two pre-existing Hook-dependency warnings.

- A rich, repeatable development-only demo data pack was created and seeded on 2026-08-28 without changing schema, migrations, RLS, RPCs, triggers, or production business rules. `npm run demo:data:check`, `npm run demo:data:seed`, and `npm run demo:data:reset` operate on fixed scoped IDs, synthetic `@demo-data.cpgrams.in` Auth users, `[DEMO]` reference data, and exact private Storage paths. Seed/reset require explicit development target/confirmation values and server-only credentials; neither the service-role key nor demo password enters source or browser code. The connected development project now has 14 profiles, 5 demo organizations, 6 hierarchical taxonomy rows, 31 grievances, 14 resolution rows (12 final and 2 interim), 6 appeals (2 decided), 3 stored systemic clusters, and 8 targeted notifications. The existing assignment trigger produced multi-GRO organization/location distribution, and the existing deterministic priority contract generated factual priority reasons. Citizen, GRO, Nodal, and Appellate workspaces are populated. The public dashboard remains intentionally honest rather than publishing aggregates because no approved anonymous aggregate contract exists. Two consecutive live seed runs returned the identical inventory and GRO workload distribution, proving scoped repeatability. Verification passes: `npm run demo:data:check`, focused ESLint, `npm run build`, `npx tsc --noEmit`, and `npm run test` (140 tests, 405 assertions). See `docs/DEMO_DATA.md` for safeguards, accounts, commands, counts, scenario references, and the public-aggregate limitation.

- Remaining public-content surfaces were completed on 2026-08-28 without changing Supabase, grievance logic, RLS, or the priority engine. `/faq` is now a searchable, section-filtered resource that clearly labels prototype behavior and covers filing, problem-first intake, limited public tracking, status and Action Required meaning, the prototype 21-day target, clarification/documents, resolution versus citizen confirmation, appeals, RTI/court/service/suggestion guidance, and password/OTP help. `/contact` deliberately says “Demo contact/support information not configured” rather than fabricating official contact details or accepting an unsupported contact form. New public `/sitemap`, `/disclaimer`, `/privacy`, and `/accessibility` routes are linked in the shared footer, which now prominently states: “Demonstration interface — not an official Government of India website.” `PUBLIC_SITEMAP` contains only public content/tracking/reporting/directory/policy routes; private workspaces remain excluded. Focused tests cover FAQ search/filter/inventory and sitemap privacy boundaries. Verification passes: `npm run build`, `npx tsc --noEmit`, `npm run test` (134 tests, 358 assertions), and focused ESLint. Repository-wide `git diff --check` still reports pre-existing trailing whitespace in the separately modified `AppHeader.tsx`; this public-content pass did not change that file. The in-app browser blocks localhost (`ERR_BLOCKED_BY_CLIENT`), so 360/390/768/desktop review was static/compiled: public layouts are single-column first with existing breakpoint grids, and the grievance builder remains single-column below its `sm` step grid; deployed-browser visual acceptance is still required.

- FAQ search suggestions were corrected on 2026-08-28 without touching any workflow or data contract. Every displayed suggestion is now defined once in `FAQ_SEARCH_SUGGESTIONS` and tested to return a relevant entry: Filing process, Document requests, Email OTP login, and Appeal. Search continues to match a question, answer, section, and explicit keywords, including “process for filing,” requested-document/upload terms, and safe email-OTP troubleshooting guidance. Focused FAQ tests, TypeScript, and production build pass.

- The original secure server-side AI gateway and citizen guidance assistant foundation was implemented on 2026-08-28 without changing schema or RLS. Its provider abstraction, strict JSON Schema plus Zod validation, prompt versioning, bounded timeout/retry behavior, direct-identifier redaction, per-caller rate limiting, safety checks, and mandatory `ai_runs` audit metadata remain in use. The earlier optional private-case chatbot context described by that historical implementation has been deliberately removed by the final AI pass: guidance is navigation-only, while private officer AI tasks retain caller-scoped RLS plus task-specific authority. See `docs/AI_ARCHITECTURE.md` for the current contract.

- I18N-01 through I18N-03 now have a multilingual foundation as of 2026-08-28. `LanguageProvider` adds the same English/Hindi/Tamil display-language selector to every shared application header. For visitors it stores a safe browser-local preference; for authenticated users it mirrors the preference into the existing self-service-only `profiles.preferred_language` field and retains the local fallback if a profile save fails. It never touches or reloads a new-grievance draft. Deployed migration `20260828130000_multilingual_foundation.sql` adds only `grievances.original_language`, a constrained best-effort BCP 47 source-language tag; new grievance filing detects English/Devanagari/Tamil while preserving `original_text`, requested outcome, clarification, messages, and resolution text verbatim. Historical rows remain `und` rather than receiving a guessed language. `translation.ts`, `TranslatedText`, and `docs/TRANSLATION_CONTRACT.md` define a bounded successful in-memory display cache and translated-text/original-text toggle for citizen and office message/resolution displays. Translation now uses the same server-only `ai-gateway`; it returns a provider translation only when configured and otherwise safely keeps the authoritative original text. Focused tests cover preference persistence, English/Hindi/Tamil Unicode preservation, source detection, English-officer-reply translated display, original toggle, cache, failure fallback, and draft preservation. Voice remains deliberately out of scope. Verification passes: deployed migration/schema check, `npx tsc --noEmit`, `npm run test` (118 tests, 309 assertions), and `npm run build`; focused ESLint has no errors and one existing Fast Refresh advisory for the context hook.

- PUBLIC-01 and PUBLIC-02 are repaired and deployed on 2026-08-28 as `20260828120000_public_tracking_safe_lookup.sql`. `/track` and `/appeal-status` remain public fallback routes (including the homepage Track Grievance entry point); they use only `public_track_grievance` and `public_track_appeal`, not direct table reads. The SECURITY DEFINER functions have an empty search path, strict reference normalization, explicit anonymous/authenticated execute grants, and a private RLS-enabled/no-grant five-minute lookup-attempt ledger that caps each hashed client fingerprint at 12 requests per minute. Missing, malformed, and throttled references all return the identical generic not-found result. The functions whitelist only generalized category, generic administrative/appeal stage, appropriate organization name, dates, and generic public milestones/status. They never return identity/contact/location, original grievance/requested outcome, documents, messages, clarification/private evidence, officer notes, raw event text/metadata, or appeal grounds/decision/evidence. New registration and appeal defaults use 80-bit random reference suffixes; existing references remain supported. Focused parser tests and an anonymous rollback-only connected-project integration prove valid/invalid records, private-field exclusion, no document/internal-event output, and the throttle. The Supabase advisor's anonymous SECURITY DEFINER notice is expected for these deliberately narrow public endpoints; the private attempt table has no browser grants. Signed-in citizens are explicitly directed to `/citizen` for their full private workspace. `npm run build`, `npx tsc --noEmit`, and `npm run test` pass (111 tests, 287 assertions); targeted ESLint for the new files is clean, and local/remote migration history matches through `20260828120000`.

- INTAKE-01 through INTAKE-03 are repaired and INTAKE-04 is deliberately partial as of 2026-08-28. The eight-step citizen intake now shows the original citizen statement separately and preserves it verbatim while allowing corrections to the local deterministic issue, structured summary, requested outcome, detected location, and identifiers. The suggestion is visibly labeled deterministic development assistance—not AI or a government decision—and a destination/category is never submitted until the citizen explicitly confirms the suggestion or a manual choice. Active organizations and categories are loaded from Supabase in explicit 500-row pages, then made searchable with database-derived hierarchy breadcrumbs; no client-side four/six-item demo list exists. A read-only connected-project check found 7 active organizations (5 nested) and 4 active categories (2 nested), so present completeness remains a seed-data limitation, not a query/UI cap. `scripts/import-taxonomy.mjs` and `docs/TAXONOMY_IMPORT.md` provide an explicitly development-only, reviewed verified/mock import path without fabricating official records. Focused intake/draft tests cover all input rows, hierarchy/search, review corrections, selected category retention, and original-text preservation. Verification passes: `npm run build`, `npx tsc --noEmit`, and `npm run test` (108 tests, 283 assertions). Focused lint reports only the six pre-existing Prettier errors in legacy `grievance-draft.ts` and two pre-existing Hook-dependency warnings in the intake route; the new intake files/tests and import script add no lint findings.

- Deterministic priority development scenarios P1–P14 were added on 2026-08-28 without changing the priority engine, scheduler, schema, RLS, or production seed data. `scripts/seed-priority-test-scenarios.ts` is explicitly development-target/confirmation-gated and supports a fixed `--at` snapshot plus a targeted remove command. It stores only isolated `DEV-PRIORITY-P*` fixture records and a display snapshot from the same tested deterministic rule contract; no wall-clock waiting is needed to inspect the 24h/48h, 3d/7d, SLA, citizen-wait, reminder-cap, CRITICAL, or terminal-resolved states. `tests/priority-test-scenarios.test.ts` asserts every scenario; `docs/PRIORITY_TEST_SCENARIOS.md` documents commands and expected results. The scheduled database evaluator remains authoritative and the linked rollback-only integration continues to prove CRITICAL GRO/Nodal notification persistence.

- A targeted multi-resolution confirmation repair was deployed on 2026-08-28 as `20260827190531_allow_feedback_per_resolution.sql`, with `20260828101000_reapply_feedback_resolution_confirmation.sql` retaining the same function after the existing lifecycle migration in fresh-database order. `DEV-PRIORITY-RELATED-PEER` had a legitimate earlier PARTLY confirmation and appeal, followed by a newer `RESOLUTION_PROVIDED` response. The former `citizen_confirm_resolution` implementation rejected every later choice because it checked for any feedback on the grievance. Feedback now references the particular final resolution; historical feedback was backfilled to its contemporaneous resolution, and the RPC locks the grievance then permits one citizen confirmation for the latest final resolution. A rollback-only authenticated check against that exact case successfully confirms the newer resolution, associates the new feedback/event with it, and preserves the earlier feedback. No RLS policy was broadened and no historical record was deleted.

- DOC-01 was repaired and deployed on 2026-08-27 as `20260827200000_citizen_document_upload_idempotency.sql`. The earlier browser path used a new random Storage path and separate browser writes for document metadata, checklist attachment, request completion, and `DOCUMENT_UPLOADED`; rapid repeat/retry could therefore duplicate evidence. Requested-item uploads now use the item UUID as their stable idempotency key. The authenticated atomic finalizer verifies the citizen-owned grievance and private object path, returns an already-finalized document on repeat, or writes exactly one document row, one checklist link, one event, and conditional request completion in one transaction. No Storage bucket policy or grievance RLS policy was weakened.
- Evidence reads now deduplicate repeated document IDs at the workspace boundary and use ID sets to keep citizen/requested/government/resolution/appeal sections mutually distinct. This does not hide separately supplied documents from distinct checklist items. The existing citizen checklist remains item-by-item: PPO alone is 1 of 3, Bank statement makes 2 of 3, and the request completes only after Pension order is supplied.
- Verification passes: deployed migration-history check; rollback-only authenticated document integration; `npm run build`; `npx tsc --noEmit`; `npm run test` (80 tests, 211 assertions); and focused ESLint for the touched data/citizen/office/test files. Browser file-picker acceptance remains manual.

- CASE-01 through CASE-05 were repaired and deployed on 2026-08-27 as `20260827191500_case_specific_clarifications_and_citizen_reminders.sql`. Clarification work is now represented by one unresolved, grievance-keyed `clarification_requests` row rather than inferred from a global/stale administrative state. The migration backfills only legacy cases with a real unresolved `CLARIFICATION_REQUESTED` event and does not convert document-only waits into clarification requests.
- The assignment-aware officer RPC persists the question/message, sets `CLARIFICATION_REQUIRED` only for that case, appends immutable `CLARIFICATION_REQUESTED` with pause reason `WAITING_FOR_CITIZEN_CLARIFICATION`, notifies the citizen, and immediately reevaluates priority. The citizen workspace now provides the recorded question, required response, optional private attachment, safe pending/error UI, and success state. Submission fulfills the exact request, appends `CITIZEN_RESPONDED`, clears its Action Required notification, advances to `CITIZEN_RESPONSE_RECEIVED` when no other required citizen action remains, resumes priority evaluation, and notifies the assigned GRO.
- Citizen reminders now persist through a locked, citizen-owner-only RPC. Open eligible cases receive one immutable `CITIZEN_REMINDER_SENT`, an assigned-GRO notification, and immediate deterministic priority reevaluation. A configurable 72-hour cooldown prevents rapid duplicates; the existing 14-day/15-point priority cap remains unchanged. Waiting-on-citizen and inactive cases are ineligible, and the citizen sees the next allowed reminder time.
- Verification passes: production build, TypeScript, all 78 tests/207 assertions, focused ESLint for the formatted data-access/query/generated-type/officer files, deployed migration-history check, and the rollback-only authenticated CASE integration. The integration proves two-case isolation, request/send, citizen action, response visibility, action clearing, pause/resume, priority resumption, first reminder, duplicate rejection, one event, capped score reason, and assigned-GRO notifications. Repository-wide lint still fails on the documented pre-existing formatting/legacy backlog (6,670 problems), including citizen presentation files; no unrelated lint refactor was made.

- ROUTE-04, ROUTE-05, and ROUTE-06 were completed and deployed on 2026-08-27 as `20260827163618_complete_atomic_transfer_and_wrong_route_deadline.sql`. The observed direct-update RLS failure was the intended assignment-aware `WITH CHECK` rejecting the post-transfer row after organization/assignee changed. It was not repaired by broadening RLS: the browser uses the locked, explicitly authorized transfer RPC, and all grievance policies remain unchanged.
- Atomic transfer now validates caller/source authority, transferable lifecycle state, active/different destination, and the existing GRO-root or Nodal-subtree rule. It preserves grievance/citizen/original data and relations, appends named immutable history, changes routing, runs the existing destination GRO assignment engine, records meaningful government activity, satisfies pending wrong-route work, and notifies the citizen/new assignee. Old GRO and Nodal visibility continue to follow the unchanged organization/assignment/subtree policies.
- Officers can explicitly mark “Incorrectly routed / needs transfer.” This records `wrong_route_detected_at`, an exact 48-hour `transfer_due_at`, a citizen-visible `WRONG_ROUTE_FLAGGED` event, and an action notification without transferring ownership. The case workspace shows detection time, deadline, and remaining/overdue time; an authorized transfer records `wrong_route_resolved_at`. Active/hierarchy-authorized organizations are the only transfer choices shown, but database authorization remains authoritative.
- Verification passes: production build, TypeScript, 77 tests/204 assertions, focused lint with zero errors (two pre-existing Fast Refresh warnings), both deployed rollback-only authenticated transfer/assignment suites, migration-history alignment, and `git diff --check`. Supabase advisors report no new transfer-security warning or error-level finding; remaining notices are the documented pre-existing assignment-table, opened-RPC, Auth, RLS-performance, and unused-index advisories.

- The R2 transfer security correction was deployed on 2026-08-27 as `20260827161634_restrict_officer_transfer_destinations.sql`. The public `officer_transfer_grievance` RPC is now `SECURITY INVOKER`; its privileged implementation is confined to `private.execute_officer_transfer` with an empty `search_path`, explicit authenticated/service-role execute grants, source row locking, assignment-aware source authorization, and server-side destination authorization. Assigned GROs may transfer only to non-appellate organizations in the source case's existing top-level government hierarchy; Nodal Officers may transfer only within their configured organizational subtree. Cross-root, out-of-subtree, appellate-cell, nonexistent, citizen, unrelated-GRO, and wrong-assignee attempts are rejected before ownership changes.
- The correction changes no grievance RLS policy, assignment trigger, jurisdiction rule, queue view, pagination path, or lifecycle state contract. Authorized transfers remain one transaction, append immutable `CASE_TRANSFERRED`, invoke destination auto-assignment, and notify the citizen. Citizen-visible transfer events now name both source and destination organizations in the title/description and metadata instead of relying on UUID metadata.
- The extended rollback-only integration test passed against the deployed function under the real `authenticated` role. It verifies authorized GRO and Nodal transfers, `42501` for every unauthorized destination/caller/source case, `22023` for a nonexistent destination, unchanged rows/events after rejection, immutable transfer history, destination GRO assignment, and human-readable citizen history. Supabase security advisor no longer reports the public transfer RPC as an authenticated `SECURITY DEFINER` function.

- ROUTE-01, ROUTE-02, ROUTE-03, and UX-02 were repaired on 2026-08-27. The connected project now has deterministic organization/jurisdiction-first GRO assignment, multiple active GRO assignment profiles, least-loaded distribution with stable tie-breaking, assignment-aware officer mutations, Nodal subtree visibility, and Appellate exclusion until an appeal exists. Same-organization GRO read visibility remains the existing policy, while the case workspace exposes mutations only to the assigned GRO or an authorized Nodal user.
- `/office/cases` now uses the RLS-preserving `officer_case_queue` security-invoker view for database-side exact-count pagination (25/50/100 rows), search, priority, state, authorized-organization, location, and assignee filters. Priority score, submission time, and ID provide stable queue ordering; the browser no longer loads an unbounded queue before filtering.
- Development provisioning now demonstrates multiple GROs in one organization, GROs in different organizations, and Pune/Bengaluru jurisdiction restrictions without frontend account constants. A guarded admin script created three additional demo GRO Auth/profile records and assignment configuration; credentials remain environment-only. Connected migration history includes `officer_assignment_and_queue_scaling` and `preserve_authorized_transfer_assignment`.
- Focused unit coverage proves eligibility, organization/location filtering, 8-to-4 balanced distribution, Nodal subtree expansion, and bounded ranges. The rollback-only linked test runs under the real `authenticated` role and proves GRO organization visibility plus assigned-officer action denial, Nodal subtree visibility, Appellate ordinary-case exclusion/appeal context, query bounds, transfer reassignment, and immutable history. No integration-test rows persist.
- Verification passes: `npm run build`, `npx tsc --noEmit`, all 74 tests/196 assertions, focused lint, and `git diff --check`. Supabase TypeScript types were regenerated from the connected project after migration. The security advisor reports no new error-level findings; its assignment-table no-policy notice is intentional because the table is service-role-only, and the authenticated security-definer notices cover the existing/open RPC and the narrowly authorized transfer transaction rather than blanket table access.

Last verified: 2026-08-27

- A focused repair baseline was recorded in `docs/BUG_REPAIR_STATUS.md` on 2026-08-27. It does not treat prior implementation labels or unit tests as browser acceptance. `npm run build`, `npx tsc --noEmit`, and `npm run test` pass (62 tests, 166 assertions); `npm run lint` fails with the existing repository-wide formatting backlog (8,542 problems). The in-app browser cannot open localhost in this workspace (`ERR_BLOCKED_BY_CLIENT`), so authenticated/cross-device/storage acceptance remains manual. No Supabase schema, RLS, lifecycle, priority, or product behavior was changed.

- AUTH-01 through AUTH-07 were repaired without a schema/RLS or grievance-workflow change. Browser authentication now uses ordinary browser-local Supabase session storage rather than Lovable preview session brokering; confirmation and recovery redirects use the central `/auth/callback` helper for the current CPGRAMS origin; signup supports both Supabase Confirm Email settings; the default implicit recovery flow establishes the email-link session and the callback also exchanges PKCE codes where configured; route protection distinguishes authentication initialization, profile loading, authorized, and profile-unavailable states; password fields have accessible show/hide controls and safe local/server error messages. Citizen signup now requires mobile number and stores optional gender/location only as non-authorizing Auth metadata because the approved profile schema has no corresponding fields. See `docs/AUTH_SETUP.md`. Focused auth regressions cover the initialization race, profile-loading phase, both signup outcomes, callback session handling, mismatch copy, redirect propagation, and profile-role routing. `npm run build`, `npx tsc --noEmit`, and `npm run test` pass (69 tests, 185 assertions); targeted lint has no errors, only two pre-existing Fast Refresh warnings in `session.tsx`. Final cross-device email acceptance still requires the documented Supabase dashboard configuration.

- Lovable handoff prepared on 2026-08-26 in `docs/LOVABLE_TOMORROW.md`. No product features or database changes were made in this handoff pass. Build and typecheck passed; the complete test suite passed with 55 tests and 140 assertions. The next session should focus on UI/UX and presentation surfaces while preserving all existing Supabase, RLS, route, priority, lifecycle, and data contracts.

## WORKING

- RES-01 through RES-05 and resolution-related UX-01 were repaired on 2026-08-28. Deployed migrations `20260827210000_repair_resolution_lifecycle_terminal_state.sql` and `20260827220000_backfill_confirmed_resolution_completion.sql` preserve the three independent lifecycle lanes and normalize legacy confirmed rows: officer submission records `RESOLUTION_PROVIDED` / `RESOLUTION_PROPOSED` / `AWAITING_CONFIRMATION`, saves the plain-language achieved outcome, stops the original government-processing SLA, appends `RESOLUTION_SUBMITTED`, and creates the citizen action “Review government's resolution.” It never citizen-confirms the outcome.
- Citizen YES/PARTLY/NO confirmation is refresh-durable and transactionally serialized. YES stores `CONFIRMED_RESOLVED` / `RESOLVED` and `CITIZEN_CONFIRMED_RESOLVED`; PARTLY stores both partial states plus structured disagreement and `CITIZEN_CONFIRMED_PARTLY_RESOLVED`; NO stores `NOT_RESOLVED` / `UNRESOLVED`, structured disagreement/evidence, and `CITIZEN_REJECTED_RESOLUTION`. An identical retried request is a successful no-op, the rendered handler synchronously blocks rapid double clicks, matching action notifications close, and PARTLY/NO continue to expose the existing preloaded appeal path.
- Original-case SLA presentation now measures to persisted `government_response_completed_at` and displays “Government processing complete” instead of continuing beyond the target. The unchanged active-case priority algorithm is wrapped only for terminal original-processing states: persisted priority becomes NORMAL/0, escalation and future evaluation are cleared, immutable priority history remains, and terminal records remain in the full historical queue while being excluded from active CRITICAL/HIGH/actionable dashboard counts.
- Resolution outcome and timeline copy is viewer-aware. Citizen routes use `YOU / GOVERNMENT / SYSTEM` and citizen-perspective outcome text; GRO, Nodal, and Appellate routes use `CITIZEN / GOVERNMENT / SYSTEM` and factual third-person text such as “Citizen confirmed the issue is resolved.”
- Both migrations are present in connected Supabase history. The rollback-only authenticated integration test passes against the linked project and leaves no mutations; it proves officer submission, awaiting confirmation, all three citizen choices, identical-call idempotency, notification closure, appeal filing, SLA completion, terminal priority normalization, and retained history. Unit coverage passes with 87 tests and 228 assertions. The local public page rendered in the in-app browser through the LAN address; credentialed mutation acceptance was not performed because entering the demo password requires an explicit action-time approval.

- The remaining non-AI presentation surfaces are complete without changing Supabase schema, RLS, citizen/GRO workflow logic, or the priority engine. Nodal `/office/analytics` now computes its RLS-scoped snapshot from stored grievances, appeals, persisted priorities, and append-only case events: confirmed resolution, SLA compliance, appeal, citizen-confirmed unresolved, clarification, transfer, repeat-cluster, first meaningful response, CRITICAL/HIGH, and systemic-cluster indicators. Government `DISPOSED` is never treated as a citizen-confirmed resolution.
- `/office/systemic-issues` now renders current stored/seeded clusters with organization, recorded geography, stored versus RLS-accessible case count, average age, unresolved percentage, appeal rate, and status. The page explicitly says that stored cluster data—not future AI clustering—is being shown; case-level aggregates are limited to linked grievances visible under RLS.
- Appellate list and file views now prioritize the comparison required for a manual decision: original complaint, requested outcome, government resolution, citizen disagreement/requested correction, separate citizen and government evidence, and a chronological combined case/appeal timeline. The existing manual decision/reply actions remain role-gated and no automatic decision path was added.
- The public website now has honest `/dashboard/public` and `/dashboard/appeals` reporting-status pages, replacing home-page illustrative metrics with clear non-publication status. Track Grievance and Appeal Status remain public reference-number fallbacks, but explicitly guide users to secure sign-in rather than pretending to query private cases. Directory records are consistently labeled prototype/demo—not official government data—and the contact page no longer presents placeholder contact details as official.
- `/admin` is now a minimal RoleGuard-gated technical workspace. After a platform administrator passes the centralized role gate, it reads only organizations, categories, and RLS-visible AI-run audit metadata. It offers no case, grievance, resolution, outcome, appeal-decision, or case-history capability.
- Focused supervisor presentation tests verify citizen-confirmed resolution versus disposal and RLS-limited cluster aggregation. `npx tsc --noEmit` and `npm run test` pass on 2026-08-26 (62 tests, 166 assertions); final production build is rerun as part of this handoff.

- Government Officer UI/UX refinement is complete without changing the deterministic priority engine, escalation algorithm, Supabase schema, or RLS. `/office` now separates immediate work needing attention (CRITICAL, HIGH PRIORITY, SLA RISK, WAITING FOR OFFICER) from queue context (WAITING FOR CITIZEN, NEWLY ASSIGNED, POSSIBLE RELATED CASES). Its actionable queue excludes cases whose persisted priority row records `waiting_on_citizen`, so paused work is not represented as officer neglect.
- `/office/cases` retains the full RLS-authorized queue and now shows the persisted last meaningful government action rather than generic case activity. Priority remains sorted by recorded level and score, has expandable real `priority_reasons`, and gives waiting-on-citizen cases an explicit “Government inactivity escalation paused” explanation. The tablet-safe table continues to retain its horizontal-scroll behavior while progressively hiding secondary columns.
- `/office/cases/$id` is organized around the citizen's original problem, requested outcome, current case state, case context, officer actions, timeline, private evidence, clarification/document requests, and interim updates/resolutions. Authorized private evidence uses the existing RLS-authorized signed-URL control. The escalation panel reports only persisted priority/SLA timing, engine audience rules, and the signed-in user's own notification state; it explicitly states that escalation does not transfer legal ownership.
- The manual document-request builder now clearly identifies its citizen-facing checklist, required/optional items, and confirm/send action. Its future AI-assisted checklist control remains a visible placeholder. The Resolution Quality surface is now connected to the advisory `AI_RESOLUTION_COMPARE` workflow, while the resolution composer preserves its existing required fields, manual submission authority, and separate citizen confirmation lifecycle.
- Focused officer presentation tests cover citizen-wait classification, the persisted meaningful-action display, and the RLS-scoped escalation notification presentation. `npx tsc --noEmit`, `npm run build`, and `npm run test` pass on 2026-08-26 (60 tests, 156 assertions). Browser visual acceptance remains a manual check; no browser session was used to mutate live records.

- Citizen presentation refinement now adds a reusable `CaseJourney` and `CaseNarrativeCard` to the authenticated case workspace. The journey derives its current/completed stage from existing grievance, outcome-confirmation, and appeal data; the narrative uses only recorded owner, event, government-state, citizen-confirmation, and action-required facts. It does not create AI text, infer government activity, or merge the separate lifecycle fields.
- The citizen workspace now places Action Required ahead of status detail, keeps the original grievance text in its own “Your problem” section, and shows `requested_outcome` separately as “What you asked government to do.” Citizen document requests show required/optional state and upload progress; timeline entries visibly identify You, Government, or System without color-only meaning. Dashboard cases requiring citizen action sort first, and grouped notifications put action-required updates first.
- The resolution review now displays original grievance text separately from the requested outcome. The eight-step citizen intake progress indicator has accessible current-step semantics and uses the informational lane for completed workflow steps; green remains reserved for citizen-confirmed success. Focused narrative tests, the complete 57-test suite, typecheck, and production build pass.

- The citizen New Grievance wizard destination Confirm repair is complete. The prior control only set display-derived taxonomy fields and did not advance the wizard. The deterministic interpretation contract now carries real suggested organization/category/subcategory IDs; Confirm validates those IDs, commits them to the one persisted draft, marks the destination confirmed, and advances directly from step 5 to Completion Review (step 6). Manual Change remains available and has its own explicit confirmation control and validation message.
- The wizard no longer owns its current step separately from its persisted draft. `NewGrievanceDraft` v2 stores `currentStep`, confirmed taxonomy choice, and existing safe text/interpretation fields in user-scoped browser local storage (`cpgrams:new-grievance:<user-id>`). The hydration effect is keyed only by authenticated user ID, preventing normal AuthProvider/profile refreshes during focus/visibility changes from overwriting active state. Refresh and route remount restore the same draft and step; drafts clear only after successful submission or an explicit future discard action, never on focus, visibility change, or logout.
- Focused draft tests cover suggested/manual destination commit, missing-suggestion validation, exact step restoration, v1-draft migration, and user-scoped storage keys. `npm run build`, `npx tsc --noEmit`, and `npm run test` pass (44 tests, 112 assertions). Browser acceptance remains a required manual check.

- Appeal-reply authorization was repaired after live browser testing exposed a GRO rejection. The exact cause was `FOR UPDATE` in `officer_reply_to_appeal`: GROs correctly have no `appeals` update policy, so PostgreSQL RLS suppressed the selected appeal row before the function reached its organization check. Migration `20260826080322_fix_office_appeal_reply_scope.sql` removes that unnecessary lock because a reply only appends records; it does not update the appeal.
- Reply-request routing is now explicit and consistent. `APPEAL_OFFICE_REPLY_REQUESTED` metadata records `requested_organization_id`; the request notifies the grievance's assigned responsible officer, and a GRO must match that exact office while a Nodal user must have existing organization-tree scope. The reply notifies the exact Appellate requester recorded on that immutable request event. Migration `20260826080712_fix_appeal_reply_recipients.sql` avoids cross-profile enumeration that RLS correctly prevented.
- A rollback-only connected-project integration test (`supabase/tests/appeal_reply_scope.sql`) passed under the real `authenticated` database role: it verifies the requested-office metadata, target GRO notification, authorized GRO and authorized Nodal replies, Appellate notification, unchanged grievance administrative state, and denial of an unrelated GRO. No test data persists.

- The focused appeal-workflow repair is deployed: the Nodal dashboard's “Review these cases” control now navigates to `/office/cases?attention=appeal`, an RLS-authorized supervisory case filter. Nodal users remain unable to enter `/office/appeals` or make appellate decisions.
- Appellate Authority appeal files now provide real manual “Record your decision” and “Ask the office for a reply” forms. The authenticated `appellate_record_appeal_decision` transaction updates only the appeal to `DECIDED`, appends immutable `APPEAL_DECIDED` appeal and case events, and notifies the citizen. It does not alter the grievance's separate outcome or citizen-confirmation states.
- A reply request moves the appeal to `UNDER_REVIEW`, persists an internal message, appends immutable `APPEAL_OFFICE_REPLY_REQUESTED` events, and notifies GRO profiles in the responsible organization. An authorized GRO/Nodal reply persists the message, appends `APPEAL_OFFICE_REPLY_PROVIDED` events, and notifies the assigned Appellate Authority organization. The Appellate file renders that activity from real authorized appeal events.
- Deployed migration `20260826072916_appeal_workflow_actions.sql` adds only three `SECURITY INVOKER` RPC functions with role and organization checks. It adds no tables, no RLS policy changes, and no service-role/browser access.

- The single TanStack Start/Vite application builds successfully and TypeScript type checking succeeds.
- Supabase client configuration exists in `src/integrations/supabase/client.ts` and uses the Vite URL and publishable-key variables.
- Migrations define the reported core tables: profiles, organizations, grievance categories, grievances, case events, documents, document requests and items, messages, resolutions, feedback, appeals and appeal events, notifications, AI runs, issue clusters, and issue-cluster members.
- The `grievances` migration keeps `administrative_state`, `outcome_state`, and `citizen_confirmation_state` as separate enum-backed fields.
- `case_events` and `appeal_events` only receive `SELECT` and `INSERT` grants/policies; no update or delete policy is defined for authenticated users.
- Migrations contain citizen ownership, GRO organization, nodal organization-subtree, and appellate organization access rules. The follow-up migration moves policy helper functions into the private schema and recreates the affected policies.
- Reference seed data includes Streetlight and Pension categories/organizations and systemic issue clusters.
- `AuthProvider` restores the browser session, subscribes to `onAuthStateChange`, fetches the signed-in user's RLS-protected `profiles` row, and derives authorization from `profiles.role`.
- Citizen and officer sign-in share the same Supabase Auth system, have no role selector, and route only after reading the authenticated user's RLS-protected `profiles.role`. Email OTP is the primary login tab and email/password remains available.
- `AuthProvider` restores a browser session, reloads the corresponding profile, and preserves the session across refreshes. Logout clears the Supabase session.
- Public citizen signup uses real Supabase email/password signup metadata without a role field. The database signup trigger creates the linked `profiles.id = auth.users.id` profile with its default `citizen` role, and an established session redirects to `/citizen`.
- Login OTP uses native `signInWithOtp({ options: { shouldCreateUser: false } })` and `verifyOtp({ type: "email" })`; it never creates a custom OTP, stores a code, or infers a role from an email address. Supabase currently delivers an eight-digit OTP in the configured project, so the shared login/recovery UI and validation now accept the complete eight-digit token. The form supports paste/autofill, safe errors, a 60-second resend countdown, and duplicate-submit prevention.
- Password recovery uses native `resetPasswordForEmail`, verifies the emailed code with `verifyOtp({ type: "recovery" })`, and permits `updateUser({ password })` only after that component instance has completed recovery verification. The auth layout deliberately keeps the recovery route mounted while Supabase establishes the short-lived recovery session.
- Officer sign-in uses the same Supabase OTP/password authentication and has no role selector; the destination is determined from the profile role.
- The profile-role update migration is deployed to the connected Supabase project: authenticated users can update only their own `full_name`, `phone`, and `preferred_language`, not their role or organization. The deployed follow-up migration removes the existing blanket platform-admin grievance/appeal access and keeps appellate original-grievance access read-only.
- The central `PROTECTED_ROUTE_RULES` map independently guards nested routes. GRO/nodal can access cases, nodal can access analytics/systemic issues, appellate can access appeals, citizen can access `/citizen/*`, and platform administrators land at the protected `/admin` placeholder. Unauthorized users are redirected to the appropriate sign-in page or their own workspace.
- `src/lib/cpgrams/data-access.ts` is the typed browser data-access boundary for profiles, grievances, case events, document requests and items, documents, messages, resolutions, feedback, appeals and appeal events, notifications, and issue clusters. It uses the generated `Database` types and the publishable Supabase client, so every query remains subject to RLS.
- `src/lib/cpgrams/queries.ts` centralizes React Query keys and hooks; `src/lib/cpgrams/data-adapters.ts` converts database enums and rows to the existing presentation model without combining administrative, outcome, and citizen-confirmation state.
- Authenticated citizen screens now read the signed-in user's profile, owned grievance collection, full case record, citizen-visible case events/messages, document requests and items, documents, resolutions, feedback, appeals, and notifications from Supabase. They do not silently substitute fixture data after a failed request.
- Authenticated office screens now read the RLS-authorized grievance queue and case records, authorized appeals, and role-gated issue clusters from Supabase. Office dashboard and analytics counts are calculated from the authorized query result rather than hard-coded values.
- Authenticated data screens include explicit loading, empty/not-found, and retryable error states. Live empty collections such as appeals and documents render as empty states rather than demo records.
- A linked read-only database check on 2026-08-25 confirmed the seeded `CPG-2026-STREETLT` Streetlight case and `CPG-2026-PENSION1` Pension case, both owned by the citizen demo profile. It also confirmed 9 case events, 1 document request, 1 message, 1 resolution, 2 notifications, and 2 issue clusters. These records now flow through the same queries used by the UI.
- The authenticated route scan contains no `sample-data.ts` imports. The fixture module remains for the public officer directory and any future tests/story/demo utilities.
- Current verification passes: `npm run build`, `npx tsc --noEmit`, and `npm run test` (36 tests, 101 assertions, including deterministic priority/escalation rules and all earlier authentication, citizen-case, resolution, evidence, and route coverage).
- The citizen dashboard is now data-driven: it shows Active Grievances, Action Required, Awaiting Resolution Review, and Appeals in Progress; searches real title, registration-number, and category fields; and filters All, Active, Action Required, Resolution Review, Appealed, and Closed without requiring a citizen to enter a tracking number.
- Citizen case workspaces now display the preserved original grievance, structured fields available in the current schema (category, urgency, location, requested outcome), current organization/state, last update, citizen-visible timeline, deterministic action state, document-request progress, and separate Citizen, Requested, Government, Resolution, and Appeal evidence sections.
- The live private `grievance-documents` bucket was verified as non-public. The citizen document-request checklist uploads new files only to the authenticated user's private folder, creates an RLS-protected `documents` record, links the request item, completes a request when all required items are supplied, and appends a citizen-visible immutable case event. It uses unique paths and no Storage upsert or service-role key.
- Deterministic tests cover the two golden scenarios: `CPG-2026-PENSION1` produces Upload Documents, and `CPG-2026-STREETLT` produces Review Government Resolution. A linked read-only check confirmed their live state fields match those scenarios.
- The authenticated citizen new-grievance flow is an eight-step, citizen-language-first workflow: describe what happened; review an interpretation; add important information; define the requested outcome; confirm or change a suggested destination; completion review; final review; and submit. Manual taxonomy selection appears only after the citizen selects Change.
- New grievance drafts are restored and saved per authenticated user in browser local storage during completion. The original citizen text and requested outcome are persisted separately when submitted.
- The deployed `20260825175136_idempotent_grievance_submission.sql` migration adds a citizen-scoped submission key and a database trigger. A repeated browser submission resolves the existing grievance instead of creating another one; each newly persisted grievance receives exactly one immutable, citizen-visible `GRIEVANCE_SUBMITTED` event and the database-generated registration number.
- Submitted citizens are redirected to the RLS-protected `/citizen/grievances/$id/submitted` confirmation page, then can open the case or return to the data-driven dashboard.
- `src/lib/cpgrams/ai-contracts.ts` defines the stable future interpretation contract. Today `deterministicInterpretationAdapter` is a local, deterministic taxonomy suggestion only; it creates no government activity or binding administrative decision, and an unavailable interpretation still permits manual submission.
- The office dashboard now derives Critical/Urgent, High Priority, SLA Risk, Waiting for Officer, Waiting for Citizen, Newly Assigned, and Possible Related Cases from the RLS-authorized queue. The case queue shows grievance, category, location, age, SLA, current action state, priority, last activity, and assignment state.
- The deployed `20260825201506_deterministic_priority_auto_escalation.sql` migration adds a non-AI, auditable priority engine. `grievance_priorities` maintains score, NORMAL/ELEVATED/HIGH/CRITICAL level, human-readable reasons, assignment/open/meaningful-action timestamps, escalation level, next threshold, and citizen-wait pause state. The private configuration row holds every threshold and score contribution; no product component contains authority to recalculate or overwrite priority.
- Supabase Cron runs `private.evaluate_all_grievance_priorities()` every 15 minutes. The rules cover 24/48-hour unopened cases, 3/7-day stalled cases, 50/75/90/100-percent SLA progression, a 14-day reminder window with a 15-point cap, related grievances, and active appeals. Waiting on required citizen documents, clarification, or resolution confirmation pauses government-inactivity contributions without suppressing independent SLA/appeal factors.
- Priority/escalation appends internal immutable `PRIORITY_CHANGED` and `ESCALATION_TRIGGERED` events. HIGH notifies the assigned/responsible GRO; CRITICAL or an SLA breach additionally notifies authorized Nodal profiles. Escalation never updates organization, assigned officer, or any of the three grievance lifecycle states.
- The office dashboard and queue consume RLS-filtered persisted priority rows, sort by level then score, and display “Why this priority?” reasons. Opening an authorized office case calls the idempotent `officer_mark_grievance_opened` RPC; the case workspace shows the same priority explanation and reports an open-tracking error rather than hiding it.
- Linked verification on 2026-08-26 confirmed the migration in remote history, the active `*/15 * * * *` Cron job and its first successful scheduled run, persisted priority rows for all three current grievances, expected calculator results for unopened, breached-SLA, waiting-on-citizen, and reminder-cap scenarios, and no database lint/security/performance advisor errors. A rollback-only linked integration test confirmed CRITICAL creates one escalation event plus responsible-officer and Nodal notifications without retaining test mutations.
- The officer case workspace now shows the preserved citizen grievance, available structured summary, requested outcome, current routing, timeline, relevant private evidence, and citizen-required action. GRO/nodal users can request clarification or documents, add interim updates, transfer, attach evidence, and submit a resolution from the workspace.
- The deployed `20260825181035_officer_case_workflows.sql` migration adds structured resolution/interim detail fields and RLS-invoker transactional functions. Document and clarification requests create citizen-visible actions, immutable events, and in-app notifications. A resolution writes the `resolutions` record, sets only `administrative_state = RESOLUTION_PROVIDED`, `outcome_state = RESOLUTION_PROPOSED`, and `citizen_confirmation_state = AWAITING_CONFIRMATION`, appends `RESOLUTION_SUBMITTED`, and notifies the citizen. It never marks the citizen outcome resolved.
- Officers manually build document-request checklist items (label, description, required/optional). The disabled “AI: Convert to checklist” control is an explicit future advisory hook only; no AI feature has been implemented.
- Citizens can now review a government resolution against their original request, the government action/evidence, and their next step. YES records `CONFIRMED_RESOLVED` plus `outcome_state = RESOLVED`; PARTLY and NO capture structured disagreement and optional evidence without treating a government claim as a citizen-confirmed outcome.
- The deployed `20260825184054_citizen_resolution_appeal_lifecycle.sql` migration uses citizen-only, RLS-invoker transactions for confirmation and appeal filing. It appends the appropriate immutable confirmation event; appeals use the case's assigned Appellate Authority, generate the database reference number, append `APPEAL_CREATED` to both case and appeal histories, and set only `administrative_state = APPEAL_FILED`.
- Appeals are visible in the real, RLS-authorized Appellate Authority workspace with original grievance, requested outcome, complete timeline, government resolution, citizen reason/requested correction, and all authorised evidence. Appellate decisions remain manual.
- `src/lib/cpgrams/appeal-summary-contract.ts` reserves a typed, advisory-only future appeal-summary interface. Citizens continue to submit their own written appeal text; no appeal AI is implemented.
- The citizen resolution confirmation route now uses an actual `form onSubmit` path: submit button → validation → optional citizen-evidence upload → `citizen_confirm_resolution` RPC → active case/dashboard query refetch → explicit saved/error UI. The deployed `20260825184054` migration is present in remote history. Its transaction persists YES as `CONFIRMED_RESOLVED` plus `RESOLVED` and appends `CITIZEN_CONFIRMED_RESOLVED`; PARTLY/NO persist the citizen's structured disagreement and append their own immutable events.
- Citizen dashboard current actions are grouped **by grievance ID**, not by action type. Each case group displays its title/registration number and every unresolved task for that case; the Updates screen also groups notifications by their `grievance_id`. The historical case timeline remains a separate chronological record.
- Resolution confirmation drafts are saved synchronously per authenticated user and grievance in browser session storage. They survive navigating away and reopening in the same session, and clear only after successful confirmation or the explicit Discard draft action.
- Resolution Evidence deterministically contains RLS-authorized, non-citizen documents whose `doc_kind` is `government_evidence` or `resolution_evidence`. Citizen, requested, government, and appeal evidence remain separate case sections without duplicate rendering.
- The rendered private-document control opens a blank tab synchronously from the click (preventing browser pop-up blocking), clears its opener, then navigates it to a 60-second signed URL created only after an RLS-authorized document-row lookup and private Storage authorization. It closes the tab and renders a safe error if URL creation fails.
- Focused verification passes: `npm run build`, `npx tsc --noEmit`, and `npm run test` (36 tests, 101 assertions), including the child-route render boundary, resolution lifecycle, priority calculation, waiting-on-citizen pause, reminder cap, reason generation, critical notification audience, evidence mapping, and appeal preload.
- The repeated Confirm Outcome browser failure was a TanStack Router render-boundary bug, not an RPC/form failure. The generated route tree correctly matched `/citizen/grievances/$id/resolution` as a child of `/citizen/grievances/$id`, but `citizen.grievances.$id.tsx` rendered only the grievance detail and no `<Outlet />`; therefore the URL changed while the resolution component never mounted. The parent now renders the detail only when it is the leaf match and renders `<Outlet />` for resolution, appeal, and submitted child matches.
- Development-only resolution diagnostics emit `[resolution-route:01..06]` for navigation/mount/query/render and `[resolution-confirm:01..14]` for button/form/validation/evidence/RPC/refetch/success/navigation. They log only identifiers, lifecycle states, role, and safe error metadata; production builds emit nothing.
- A live read-only database check on 2026-08-26 confirmed the deployed `citizen_confirm_resolution` signature exactly matches the browser call: `p_grievance_id uuid`, `p_confirmation citizen_confirmation_state`, the three structured text arguments, and `p_evidence_document_id uuid`; it returns `void` and is security invoker. The seeded Streetlight case remains `RESOLUTION_PROVIDED` / `RESOLUTION_PROPOSED` / `AWAITING_CONFIRMATION`, has a current resolution, and has no feedback or citizen-confirmation event, proving the prior browser attempt did not mutate it.

## PARTIALLY WORKING

- Production email OTP and recovery wiring is complete in the application and covered by mocked Supabase Auth contract tests. The earlier frontend assumed six digits while the configured project delivered eight; one shared `AUTH_OTP_LENGTH = 8` now controls login and recovery slots, validation, button enablement, and paste limiting. Hosted delivery still requires the connected project to configure Resend as Supabase custom SMTP and render `{{ .Token }}` in the login and recovery templates; no real email was sent during automated validation. Focused auth lint, `npx tsc --noEmit`, all 55 tests, and `npm run build` pass. Repository-wide lint still has the pre-existing formatting failures recorded under Known Issues. See `docs/AUTH_EMAIL_SETUP.md`.

- Appeal workflow browser acceptance is still required: manually verify the Nodal dashboard review button opens the filtered case queue, that an Appellate Authority can save a decision/request a reply, and that an authorized GRO/Nodal can reply from the case workspace. `npm run build`, `npx tsc --noEmit`, and `npm run test` pass (38 tests, 106 assertions). Remote database lint reports one pre-existing warning in `private.calculate_grievance_priority`; it is unrelated to this appeal migration.

- Stabilization verification on 2026-08-26 found that the previous resolution-route repair was incomplete: the rendered form reached the live `citizen_confirm_resolution` RPC, but PostgreSQL rejected the function's `outcome_state` assignment because its `CASE` expression was inferred as `text`. Deployed migration `20260826063043_fix_citizen_resolution_outcome_enum_cast.sql` makes each result an explicit `public.outcome_state`. Rollback-only linked checks now prove YES persists `CONFIRMED_RESOLVED`/`RESOLVED` plus `CITIZEN_CONFIRMED_RESOLVED`, while PARTLY/NO persist structured feedback; NO can create an appeal visible to its assigned Appellate Authority. The browser client could not open localhost (`ERR_BLOCKED_BY_CLIENT`), so final human browser acceptance remains required.
- Deployed migration `20260826063338_restrict_citizen_private_document_visibility.sql` aligns `documents` and private Storage object reads: citizens can read their own uploads or rows explicitly marked `citizen_visible`; authorized staff keep their existing case access. The case workspace now keeps `resolution_evidence` in Resolution Evidence, legacy `government_evidence` in Government Documents, and shows both as authorised supporting evidence during a resolution review. Appellate evidence cards now use the same signed-URL open control.
- Linked RLS verification confirms the GRO demo profile can see the ULB-PMC Streetlight case but not the unrelated CPAO Pension case; the MOHUA Nodal profile can see the ULB-PMC child case but not the unrelated pension tree. This is intended organization-tree isolation, not a reason to broaden RLS. The rollback-only test also proves a citizen cannot read an officer document with `citizen_visible = false`.
- `scripts/seed-priority-fixtures.mjs` provides 11 idempotent, explicitly named development-only `[TEST PRIORITY]` cases. It requires `PRIORITY_FIXTURES_CONFIRM=development`, refuses `NODE_ENV=production`, uses the server-only service key, and is never bundled into the client. Their real persisted results cover unopened 25/49 hours, stalled 4/8 days, SLA 75%/breach, waiting on citizen, reminder cap, related cases, and appeal attention. A second evaluator run left fixture `PRIORITY_CHANGED` (14), `ESCALATION_TRIGGERED` (6), and priority notifications (8) unchanged; no ownership or lifecycle fields were changed by evaluation.

- A server admin client exists but has no application consumers. The authenticated product data layer does not import it and all browser reads use the publishable client with RLS.
- Storage access policies and the private `grievance-documents` bucket exist. Browser downloads use short-lived signed URLs after an RLS-authorized document-row lookup; end-to-end browser upload/download still needs a manual authenticated click-through because this workspace's browser runtime cannot start.
- The prior Prompt 6 repair report did not match subsequent manual browser testing. The in-app browser can now reach the local public application, but authenticated acceptance remains pending because entering the demo password requires explicit action-time approval. Build/tests and the read-only database checks are not being treated as browser acceptance.
- Development signup becomes session-backed only after Supabase Auth **Confirm Email** is disabled in the connected project's dashboard. With that setting off, Supabase implicitly confirms the email, sends no confirmation email, and returns a session; otherwise the UI safely explains the missing session instead of leaving a successful signup on the auth page.
- `scripts/configure-demo-auth.mjs` can update existing demo users (and any existing `platform_admin` profile) using a locally supplied service-role key and `DEMO_AUTH_PASSWORD`. It is development-only and is not imported by the client. This data-wiring change did not invoke it or re-test demo credentials.
- Profile editing and appellate decisions remain explicitly unwired placeholders. Private document upload is implemented for citizens and officers; private document open/download is wired to signed URLs, but end-to-end authenticated mutation testing remains pending a usable browser runtime.

## NOT IMPLEMENTED

- Supabase mutation workflows for document download and appellate decisions.
- Server-side route authorization beyond the client guard. Any future server function must use the existing `requireSupabaseAuth` middleware and rely on RLS for data access.
- Platform administration mutation/configuration workflows. The existing `/admin` route is intentionally read-only and cannot operate on grievance lifecycle records.

## KNOWN ISSUES

- `npx supabase db lint --linked` reports one non-failing warning in `private.calculate_grievance_priority`: PostgreSQL sees the initial `'NORMAL'` local variable value as text before assignment to `public.priority_level`. Linked evaluations of all 14 current cases and the development fixtures succeed; this stabilization pass did not rewrite the large deployed priority function without a runtime failure.

- `.env` is tracked by Git despite the current `.gitignore` entry (ignore rules do not affect already tracked files), and it currently includes `SUPABASE_SERVICE_ROLE_KEY` and `DEMO_AUTH_PASSWORD` in addition to public configuration. Neither is referenced by the Vite browser data layer, but tracked secrets must be rotated, removed from Git tracking/history as appropriate, and replaced with a redacted example in a separately scoped security change.
- `npm run lint` currently fails with 10,297 errors and 11 warnings. Most errors are repository-wide CRLF line-ending violations from Prettier, with a smaller number of ordinary formatting findings. The files changed for email OTP/recovery pass a focused ESLint run; this task did not make a broad unrelated formatting rewrite.
- The linked CLI can query the live database and migration history. The private `grievance-documents` bucket was reported as live previously, but end-to-end authenticated upload/download has not yet been verified from the application.
- `npm run demo:auth` configured the existing four development demo accounts on 2026-08-26. An authenticated browser acceptance test still requires the user to enter the demo credential or explicitly approve its transmission; no email/OTP request was made.
- The full route matrix supplied on 2026-08-25 is represented in the current frontend authorization map for routes that exist. Object-level case/appeal authorization remains enforced by RLS. Future routes must be added explicitly to `PROTECTED_ROUTE_RULES`; roles do not inherit access from one another.
- `npm run build` prints a non-failing Vite notice that `vite-tsconfig-paths` can be replaced with Vite's native path-resolution option.
- `npm run build` also reports a non-failing client chunk-size warning. No unrelated bundling refactor was made in this data-wiring change.
- The new-grievance draft is intentionally client-side resumable state, not an authoritative database draft. It is cleared only after a successful (including idempotently replayed) submission. Future work may add server-backed drafts if cross-device continuation is required.
- Resolution evidence has no direct `resolution_id` foreign key in the current schema. Existing officer uploads use `documents.doc_kind = government_evidence` on the same grievance, so citizen resolution review correctly shows those authorized files. A future requirement to associate one file with one of several resolutions would need a separately scoped schema migration; none was necessary for this bug fix.
